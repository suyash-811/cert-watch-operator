{{- if .Values.rbacHelpers.enable }}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    app.kubernetes.io/name: {{ include "cert-watch-operator.name" . }}
    helm.sh/chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    app.kubernetes.io/instance: {{ .Release.Name }}
  name: {{ include "cert-watch-operator.resourceName" (dict "suffix" "certificatewatcher-admin-role" "context" $) }}
rules:
- apiGroups:
  - monitoring.sonaw.net
  resources:
  - certificatewatchers
  verbs:
  - '*'
- apiGroups:
  - monitoring.sonaw.net
  resources:
  - certificatewatchers/status
  verbs:
  - get
{{- end }}
